@*@model IEnumerable<SchemaNote_A11085.ViewModel.ColumnViewModel>
    @model IEnumerable<SchemaNote_A11085.ViewModel.ScheNoteViewModel>
    @{
        ViewData["Title"] = "List";
    }
    @{
        var count = 0;
    }
     #region Account





    @foreach (var item in Model)
    {

        <h4 style="display:inline">@item.TableName</h4>
        <input style="display:none;margin-left:15px;width:50%" type="text" value="@item.Main_UserTable" />
        <div>
            <h4>@item.TableName</h4>
            <span>使用者帳號主表:@item.Main_UserTable</span>
            <span>物件類型:資料表</span>
            <span>結構描述名稱:@item.DescriptionName</span>
            <span>物件創建日期:@item.Object_CreateDay</span>
            <span>物件修改日期:@item.Object_UpdateDay</span>
            <h4 style="display:inline;position:absolute;right:5px;">筆數 : @item.TotalCount</h4>

        </div>
    }
    <table class="table" style="border:3px #cccccc solid;" cellpadding="10" border='1'>
        <thead>
            <tr>
                <th>欄位名稱</th>
                <th>欄位說明</th>
                <th>資料型態:</th>
                <th>主鍵</th>
                <th>不為Null</th>
                <th>預設值</th>
                <th>備註</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                if (item.Column_Name == "schoolID")
                {
                    break;
                }
                <tr>
                    <td>@item.Column_Name</td>
                    <td>@item.Column_Description</td>
                    <td>@item.Column_PK</td>
                    <td>@item.Column_Type</td>
                    <td>@item.Column_IsNullable</td>
                    <td>@item.Column_Default</td>
                    <td>@item.Column_Remark</td>
                </tr>
            }
        </tbody>
    </table>
     #endregion
     #region Account_ETDS
    <h4>Account_ETDS</h4>
    <P>使用者帳號主表</P>
    <p>物件類型</p>
    <p>結構描述名稱</p>
    <p>物件創建日期</p>
    <p>物件修改日期</p>

    <table class="table" style="border:3px #cccccc solid;" cellpadding="10" border='1'>
        <thead>
            <tr>
                <th>欄位名稱</th>
                <th>欄位說明</th>
                <th>資料型態</th>
                <th>主鍵</th>
                <th>不為Null</th>
                <th>預設值</th>
                <th>備註</th>
            </tr>
        </thead>

        <tbody>

            @foreach (var item in Model)
            {
                //Console.WriteLine(item);

                if (item.Column_Name == "schoolID")
                {
                    count++;
                }
                if (count == 1 && item.Column_Name != "ID")
                {
                    <tr>
                        <td>@item.Column_Name</td>
                        <td>@item.Column_Description</td>
                        <td>@item.Column_PK</td>
                        <td>@item.Column_Type</td>
                        <td>@item.Column_IsNullable</td>
                        <td>@item.Column_Default</td>
                        <td>@item.Column_Remark</td>
                    </tr>
                }
                else if (item.Column_Name == "ID")
                {
                    break;
                }

            }
        </tbody>
    </table>
     #endregion
     #region Account_SchInfo
    <h4>Account_SchInfo</h4>
    <P>使用者帳號主表</P>
    <p>物件類型</p>
    <p>結構描述名稱</p>
    <p>物件創建日期</p>
    <p>物件修改日期</p>

    <table class="table" style="border:3px #cccccc solid;" cellpadding="10" border='1'>
        <thead>
            <tr>
                <th>欄位名稱</th>
                <th>欄位說明</th>
                <th>資料型態</th>
                <th>主鍵</th>
                <th>不為Null</th>
                <th>預設值</th>
                <th>備註</th>
            </tr>
        </thead>

        <tbody>

            @foreach (var item in Model)
            {

                if (item.Column_Name == "ID")
                {
                    count++;
                }
                if (count == 2 && item.Column_Name != "RefuseTime")
                {
                    <tr>
                        <td>@item.Column_Name</td>
                        <td>@item.Column_Description</td>
                        <td>@item.Column_PK</td>
                        <td>@item.Column_Type</td>
                        <td>@item.Column_IsNullable</td>
                        <td>@item.Column_Default</td>
                        <td>@item.Column_Remark</td>
                    </tr>
                }
                else if (item.Column_Name == "RefuseTime")
                {
                    break;
                }
            }
        </tbody>
    </table>
     #endregion
     #region Account_SchInfoRefuse
    <h4>Account_SchInfoRefuse</h4>
    <P>使用者帳號主表</P>
    <p>物件類型</p>
    <p>結構描述名稱</p>
    <p>物件創建日期</p>
    <p>物件修改日期</p>

    <table class="table" style="border:3px #cccccc solid;" cellpadding="10" border='1'>
        <thead>
            <tr>
                <th>欄位名稱</th>
                <th>欄位說明</th>
                <th>資料型態</th>
                <th>主鍵</th>
                <th>不為Null</th>
                <th>預設值</th>
                <th>備註</th>
            </tr>
        </thead>

        <tbody>

            @foreach (var item in Model)
            {
                if (item.Column_Name == "RefuseTime")
                {
                    count++;
                }
                if (count == 3 && item.Column_Name != "StudentShip")
                {
                    <tr>
                        <td>@item.Column_Name</td>
                        <td>@item.Column_Description</td>
                        <td>@item.Column_PK</td>
                        <td>@item.Column_Type</td>
                        <td>@item.Column_IsNullable</td>
                        <td>@item.Column_Default</td>
                        <td>@item.Column_Remark</td>
                    </tr>
                }
                if (count == 3 && item.Column_Name == "StudentShip")
                {
                    <tr>
                        <td>@item.Column_Name</td>
                        <td>@item.Column_Description</td>
                        <td>@item.Column_PK</td>
                        <td>@item.Column_Type</td>
                        <td>@item.Column_IsNullable</td>
                        <td>@item.Column_Default</td>
                        <td>@item.Column_Remark</td>
                    </tr>
                    break;
                }
            }
        </tbody>
    </table>
     #endregion
     #region AccountRefuse
    <h4>AccountRefuse</h4>
    <P>使用者帳號主表</P>
    <p>物件類型</p>
    <p>結構描述名稱</p>
    <p>物件創建日期</p>
    <p>物件修改日期</p>

    <table class="table" style="border:3px #cccccc solid;" cellpadding="10" border='1'>
        <thead>
            <tr>
                <th>欄位名稱</th>
                <th>欄位說明</th>
                <th>資料型態</th>
                <th>主鍵</th>
                <th>不為Null</th>
                <th>預設值</th>
                <th>備註</th>
            </tr>
        </thead>

        <tbody>

            @foreach (var item in Model)
            {
                if (item.Column_Name == "RefuseTime")
                {
                    count++;
                }
                if (count == 5 && item.Column_Name != "Sort")
                {
                    <tr>
                        <td>@item.Column_Name</td>
                        <td>@item.Column_Description</td>
                        <td>@item.Column_PK</td>
                        <td>@item.Column_Type</td>
                        <td>@item.Column_IsNullable</td>
                        <td>@item.Column_Default</td>
                        <td>@item.Column_Remark</td>
                    </tr>
                }
                if (count == 5 && item.Column_Name == "Sort")
                {
                    <tr>
                        <td>@item.Column_Name</td>
                        <td>@item.Column_Description</td>
                        <td>@item.Column_PK</td>
                        <td>@item.Column_Type</td>
                        <td>@item.Column_IsNullable</td>
                        <td>@item.Column_Default</td>
                        <td>@item.Column_Remark</td>
                    </tr>
                    break;
                }
            }
        </tbody>
    </table>
     #endregion*@ 